/* --- Stacks --- */

// Declare empty stack array
let stack = [];

// Add the element to stack
stack.push(1);

// Append a new element to the existing stack
stack.push(2);

// Remove the last element from the stack
stack.pop();
