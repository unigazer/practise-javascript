/* --- Queue --- */

// Declare empty queue array
let queue = [];

// Add the element to the queue
queue.push(1);

// Append new element to the queue
queue.push(2);

// Remove the first element from the queue
queue.shift();